#!/usr/bin/env python3
import os
import sys

if len(sys.argv) != 2: # i.e. the script name plus a path to canonicalize
    print("usage: canonicalize <path>", file=sys.stderr)
    sys.exit(1)

print(os.path.realpath(sys.argv[1]))
