#!/bin/sh
BOOKMARKS=~/.bookmarks

SELECTION=$(awk -F "\t" '{print $1}' $BOOKMARKS | sort | rofi -dmenu -i)
if [ -n "$SELECTION" ]; then
	egrep "^$SELECTION	" $BOOKMARKS | awk -F "\t" '{print $2}' | xargs $BROWSER
fi
